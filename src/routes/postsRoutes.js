/*import express from "express";
import { listenPosts } from "../controllers/postsController.js";
//import path from 'path';
console.log(path.resolve("../controllers/postsController.js"));


const routes = (app) => {
  app.use(express.json());
  app.get("/posts", listenPosts);
};

export default routes;



/*import express from "express";

const routes = (app) => {
    // Permite que o servidor interprete requisição com o corpo no formato JSON
    app.use(express.json());
    // Rota endpoint para obter todos os posts
    app.get("/posts", listenPosts);
}

export default routes;*/
